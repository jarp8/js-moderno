let resultado;

// PI
resultado = Math.PI;

// Redondear
resultado = Math.round(2.8); // Redondear hacia arriba
resultado = Math.round(2.2); // Redondear hacia abajo
resultado = Math.round(2.5); // Redondear hacia arriba

// Redondear hacia arriba
resultado = Math.ceil(2.1);

// Redondear hacia abajo
resultado = Math.floor(2.9);

// Raíz cuadrada
resultado = Math.sqrt(144);

// Absoluto
resultado = Math.abs(-500);

// Potencia
resultado = Math.pow(2, 4);

// Minimo
resultado = Math.min(3, 5, 6, 10);

// Máximo
resultado = Math.max(3, 5, 6, 10);

// Aleatorio
// resultado = Math.random() * 20;

// Aleatorio dentro de un rango...
resultado = Math.floor(Math.random() * 30);



console.log(resultado);