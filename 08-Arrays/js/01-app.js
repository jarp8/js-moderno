const numeros = [10, 20, 30];

const meses = new Array('Enero', 'Febrero', 'Marzo');

console.log(numeros);
console.log(meses);

// Arreglo que contiene datos de todo tipo

const deTodo = ['Hola', 10, true, 'Si', null, {nombre: 'Alberto'}, [1, 2, 3]];

console.log(deTodo);